import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Team Photo — Survivor Series",
};

export default function TeamPhotoLayout({ children }: { children: React.ReactNode }) {
    return children;
}
