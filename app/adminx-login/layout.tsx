import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "AdminX Login — Survivor Series",
};

export default function AdminXLoginLayout({ children }: { children: React.ReactNode }) {
    return children;
}
